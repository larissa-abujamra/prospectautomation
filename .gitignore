# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (nunca commitar chaves). .env.example fica versionado.
.env
.env.*
!.env.example

# Estado local da Supabase CLI (gerado por `supabase link`) — não versionar.
supabase/.temp

# Repositório avulso aninhado (não faz parte deste projeto).
/prospect-automation

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gstack/
test-results/
playwright-report/

# Browser automation artifacts accidentally created at repo root.
Usersstefa.pw-meta-iso/
Usersstefameta-status-list.png

# segredos locais parqueados (verify tokens etc.)
.olivia-webhook-verify-token.txt
*-verify-token.txt
*-secret.txt
.olivia-trigger-secret.txt
.vercel
